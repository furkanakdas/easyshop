apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-depl 
  labels:
    app: order 
spec:
  replicas: 1  
  selector:
    matchLabels:
      app: order 

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1  
      maxSurge: 1        

  template:  
    metadata:
      labels:
        app: order  
    spec:
      containers:
      - name: order  
        image: order  
        imagePullPolicy: Never
        ports:
        - name: order
          containerPort: 4007
        livenessProbe:
          httpGet:
            path: /health  
            port: 4007
          initialDelaySeconds: 30  
          periodSeconds: 10         
        readinessProbe:
          httpGet:
            path: /ready  
            port: 4007
          initialDelaySeconds: 5  
          periodSeconds: 5

        resources:  
          requests:
            cpu: "250m"  
            memory: "256Mi"  
          limits:
            cpu: "1000m"
            memory: "1024Mi"
            
        envFrom:
          - configMapRef:
              name: global-config
              
        env:  
        - name: ELASTIC_PASSWORD
          valueFrom:
            secretKeyRef:
              name: easyshop-secret
              key: ELASTIC_PASSWORD
        - name: ORDER_POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: easyshop-secret
              key: ORDER_POSTGRES_PASSWORD
        - name: STRIPE_TEST_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: easyshop-secret
              key: STRIPE_TEST_SECRET_KEY