apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-review-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-review
  template:
    metadata:
      labels:
        app: postgres-review
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432

          env:
            - name: POSTGRES_USER
              value: "review_service"
            - name: POSTGRES_DB
              value: "review_db"

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: easyshop-secret
                  key: REVIEW_POSTGRES_PASSWORD

          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgres-storage
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-review-pvc
